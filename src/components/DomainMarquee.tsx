import { professionalIdentity } from '@/data/portfolio';
import { SkiperMarquee } from '@/components/skiper/SkiperPrimitives';

const DomainMarquee = () => <SkiperMarquee items={professionalIdentity} />;

export default DomainMarquee;
